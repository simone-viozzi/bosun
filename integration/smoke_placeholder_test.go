//go:build integration
// +build integration

package integration

import "testing"

func Test_Integration_Smoke_Placeholder(t *testing.T) {
	t.Parallel()
	// Will be replaced by real compose-based smoke test in follow-up issues.
}
